abstract class IRepositorySettings {
  getAll() {
    // return _repositoryProfile.getAll();
  }

  getId(id) {
    // return api.getId(id);
  }

  delete(id) {
    // return api.delete(id);
  }

  edit(obj) {
    // return api.edit(obj);
  }

  add(obj) {
    //return api.add(obj);
  }
}
